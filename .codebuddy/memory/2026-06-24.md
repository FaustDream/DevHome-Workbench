# 2026-06-24 工作日志

## ProseMirror 迁移执行

### Phase 0: 构建依赖包 ✅
- 创建 `package.json`，安装 esbuild + ProseMirror 8 模块 + highlight.js
- 编写 `build.mjs` 构建脚本
- 打包生成 `js/lib/pm.bundle.js` (467KB) 和 `js/lib/hljs.bundle.js` (245KB)
- 修改 `index.html` 引入两个 bundle 和 `css/hljs-theme.css`
- 创建 `css/hljs-theme.css`（适配暖纸背景的柔和深色代码高亮主题）

### Stage 1: 单元测试 ✅
- 创建 `tests/prosemirror-tests.mjs` — 7 组 44 条测试，全部通过

### Phase 1: 数据模型升级与迁移 ✅
- `createNote()` 新增 `doc: null` 和 `wordCount: 0` 字段
- `addCapture()` 同步新增 `doc`/`wordCount` 字段
- `ns.countWords()` — 中文每字+英文每词，去 HTML 标签
- `ns.migrateNoteDoc()` — HTML→ProseMirror JSON 单条迁移（DOMParser 解析，回退空文档）
- `ns._getMigrationSchema()` — 迁移用轻量 Schema（复用节点定义）
- `ns.migrateAllNotes()` — 全量迁移，在 `loadNotes()` 后自动触发
- `loadNotes()` 自动调用迁移并保存

### Phase 2: ProseMirror 编辑器模块 ✅
- 创建 `js/proseMirrorEditor.js`:
  - `buildSchema()` — 10 个节点 + 6 种 marks
  - `buildInputRules()` — 7 条 Markdown 输入规则（#/##/###/-/1./>/```）
  - `buildKeymap()` — Ctrl+B/I/U 快捷键
  - `createPlugins()` — baseKeymap + 自定义 + inputRules + history + dropCursor + gapCursor
  - `pmCreateEditor()` — 创建 EditorView，挂载到 DOM，onChange 回调
  - `pmDestroyEditor()` — 销毁实例
  - `pmGetDocJSON/HTML/WordCount()` — 获取文档
  - `pmSetContent()` — 设置文档
- `index.html`:
  - 引入 `js/proseMirrorEditor.js`（在 notes.js 之前）
  - `#wbNoteContent` 移除 `contenteditable` 属性
  - 新增 `#wbNoteWordCount` 字数显示元素
- `js/notes.js`:
  - `openNoteEditor()` → 笔记模式用 `pmCreateEditor`，捕获模式保留 contenteditable
  - `saveCurrentNote()` → 从 PM API 获取 docJSON/docHTML/wordCount
  - `closeNoteEditor()` → 调用 `pmDestroyEditor`

### 测试状态
- 44/44 单元测试通过
- 0 lint 错误
- 运行命令: `node --experimental-vm-modules tests/prosemirror-tests.mjs`

### 待执行
- Phase 8: Markdown 导出适配

## 2026-06-25 (续) Phase 3-7 执行

### Phase 3: 代码块 NodeView ✅
- CodeBlockView 类：语言下拉(20种)、复制按钮(📋→✅已复制)、双击编辑(blur/Ctrl+Enter保存、Escape取消)
- 注册到 EditorView nodeViews
- highlight.js 语法高亮

### Phase 4: 气泡工具栏 ✅
- bubbleToolbarPlugin：选区监听，延迟50ms防止抖动
- showBubbleToolbar/hideBubbleToolbar：选区位置计算、视口翻转
- _syncBubbleToolbarState：同步按钮激活状态
- _executeBubbleAction：bold/italic/underline/heading/bulletList/orderedList/color/blockquote/codeBlock

### Phase 5: 右键菜单精简 ✅
- 移除：h1-h6、加粗/斜体/下划线、文字颜色子菜单、ctxColorPalette
- 保留：代码块子菜单、引用块、复制、粘贴
- handleEditorMenuAction → 调用 _executeBubbleAction

### Phase 6: 字数统计 CSS ✅
- .wb-note-word-count：右下角灰色小字，上边框分隔

### Phase 7: CSS 清理 ✅
- 删除：.wb-notes-toolbar、.wb-toolbar-*、.wb-note-editable pre、pre::before
- 新增：.wb-bubble-toolbar (浮动定位+三角箭头+按钮/选择框)、.wb-codeblock (容器+工具栏+pre+textarea)、.wb-note-word-count、颜色面板 20 色

### 涉及文件
- 修改：js/proseMirrorEditor.js (+240行 CodeBlockView + 气泡工具栏)
- 修改：js/events.js (编辑器右键精简 + bindBubbleToolbar + _bindLegacyToolbarEvents)
- 修改：js/ui.js (移除旧菜单项 + _renderColorPalette + 精简 insertCodeBlock)
- 修改：index.html (旧工具栏→气泡工具栏 + 右键菜单精简 + 移除 ctxColorPalette)
- 修改：css/themes/warm-paper.css (删除旧样式 + 新增气泡/代码块/字数统计样式)
