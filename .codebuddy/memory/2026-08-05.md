# 2026-08-05 开发日志

## 12:00 - Wiki 文档细节大幅增强：方法级API/布局规范/数据流（docs）

### 任务描述
用户要求大幅增强 Wiki 文档细节：①每个方法实现逻辑/参数/返回值/异常处理；②界面布局精确到组件位置/间距/对齐/响应式断点/六态交互（hover/active/disabled/loading/空数据/错误）；③每页完整数据流（API请求响应格式/状态管理/缓存/错误处理）。

### 关键决策
1. **决策**：新增 3 章（16 方法级API/17 界面布局/18 页面数据流），而非重写既有 01-15 章
   **原因**：既有章为「概览/分析」定位，新章为「实现级参考」，补充而非替代
   **替代方案**：逐章重写 —— 放弃，范围失控且丢失既有分析
2. **决策**：界面布局规格基于真实 CSS 源码（base.css/tiles.css/workbench-layout.css/time-search.css/themes/default.css）精确提取
   **原因**：间距/圆角/断点/交互态须有实据，非臆测
3. **决策**：方法级 API 覆盖核心模块（存储层/磁贴/分页/笔记/番茄钟/消息协议/搜索），方法标注「JS 现状 / TS 目标」
   **原因**：与 Phase 2 迁移衔接，双态标注便于过渡期参照
4. **决策**：数据流章含外部 API 请求/响应格式（Open-Meteo/Bing/DUCKDUCKGO/AI 适配器）+ 缓存策略总表 + 错误处理总表
   **原因**：满足「API 格式/状态管理/缓存/错误处理」四项要求

### 变更摘要
- [新增] .codebuddy/wiki/16-方法级API参考.md — 存储层(16.1)/磁贴(16.2)/分页(16.3)/笔记(16.4)/番茄钟(16.5)/消息协议(16.6)/搜索(16.7)，每方法含实现逻辑/参数表/返回值/异常
- [新增] .codebuddy/wiki/17-界面布局与交互状态规范.md — Token 体系/断点系统/三大页面组件级规格/六态交互表/布局红线
- [新增] .codebuddy/wiki/18-页面数据流详解.md — boot 数据流/磁贴/搜索/天气/问候/favicon/番茄钟/笔记/任务/AI/弹窗/侧栏/SW + 缓存策略总表 + 错误处理总表
- [修改] .codebuddy/wiki/README.md — 索引补 16/17/18 章

### 影响范围
- 消息协议变更？否；存储结构变更？否；权限变更？否；需数据迁移？否（纯文档）
- 文档成为实现级参考，后续编码/审查可据此核对

### 待办/遗留问题
- [ ] P2：16 章为「方法级」但仅覆盖核心模块，非全部 40+ 文件；如需全覆盖可按 16 章模板扩展
- [ ] P2：17 章布局规格随 F4-F9 设置项演进需定期同步

### 质量门
- lint：仅 cSpell HINT（项目术语如 devhome/pomodoro 未入词典，既有文档已存在，非新错误）

## 11:20 - TS 重构就绪检查 + Phase 1 工程基建完成（refactor）

### 任务描述
用户要求检查技能/规则/文档，判断能否开始重构；可以则出任务列表并准备重构。检查结论：规则/技能/wiki 已就绪，但工程缺 TS 基建（无 tsconfig/tests/vitest）。据此创建 wiki/15 任务列表，并**实际完成 Phase 1 基建**。

### 关键决策
1. **决策**：产出 wiki/15-重构任务列表.md（Phase 0 就绪检查 + Phase 1-4 可执行任务 + 质量门）
   **原因**：用户要「出任务列表准备重构」；分 Phase 便于追踪
2. **决策**：立即执行 Phase 1（而非只给文档）——安装依赖 + tsconfig + vitest + eslint + src/shared 骨架 + lib 纯逻辑
   **原因**：用户要「开始重构」，基建是先决
3. **决策**：`tsconfig` 先只开 `strict`，`noUncheckedIndexedAccess`/`exactOptionalPropertyTypes` 留 Phase 3
   **原因**：避免海量索引报错淹没真实问题（wiki/14 已有此结论）
4. **决策**：`no-magic-numbers` 设 warning 且 tests 豁免；`@typescript-eslint/no-restricted-syntax` 改为 ESLint 内置 `no-restricted-syntax`
   **原因**：typescript-eslint v8 无该前缀规则；测试数据字面量不应报魔法值
5. **决策**：删除 `.eslintignore`（被 eslint.config.js ignores 取代）；`package.json` 暂不加 `"type":"module"`（仅性能警告，避免副作用）
   **原因**：消除弃用警告；谨慎改动

### 变更摘要
- [新增] .codebuddy/wiki/15-重构任务列表.md — Phase 0-4 任务清单 + 状态追踪
- [修改] .codebuddy/wiki/README.md — 索引补 15 章
- [新增] tsconfig.json / vitest.config.ts / eslint.config.js（flat config）
- [新增] src/shared/constants.ts / types.ts / messages.ts / guards.ts（判别联合+zod，R3/R19/R20）
- [新增] lib/countWords.ts / formatTaskTime.ts / computeRemaining.ts / cleanEmptyHTML.ts
- [新增] tests/lib.test.ts（18 用例）; tests/README.md
- [修改] package.json — 依赖（typescript 6.0.3/vitest 4.x/zod 4.x/@types/chrome/dompurify）+ 脚本（typecheck/test:unit/lint 指向 ts）
- [修改] build.mjs — 业务拼接 loader 'js'→'ts'
- [删除] .eslintignore

### 影响范围
- 消息协议变更？否（messages.ts 为新增协议定义，尚未接线）；存储结构变更？否；权限变更？否；需数据迁移？否
- Phase 1 为纯增量基建，不影响现有 JS 运行时；构建已验证 EXIT=0

### 待办/遗留问题
- [ ] Phase 2：storage/消息类型化 + 修复 R2 双写/R6 幂等/R7 SW 持久化
- [ ] Phase 1.7 剩余：lib 补 repairDefaultCategoryContent/normalizeTask + 测试
- [ ] P2：`package.json` 是否加 `"type":"module"` 待评估

### 质量门
- tsc --noEmit：EXIT=0；vitest：18 passed；eslint：EXIT=0；build：EXIT=0

## 10:30 - 确认技术栈为 TypeScript，同步反模式规则 TS 化（refactor/docs）

### 任务描述
用户明确：后续要重构为 TS 版本，技术栈均为 TS。据此将 `rules/coding-standards/anti-pattern-rules.md`（R1-R20）从「JS + TS 化提示」改写为「以 TS 为准」的版本，并同步 RULE.mdc §五 与 wiki/14 章的表述。

### 关键决策
1. **决策**：技术栈确认为 TS（strict），写入长期记忆 MEMORY.md 与 wiki/14 章标题声明
   **原因**：用户明确后续重构目标为 TS；新代码须按 TS 编写，禁止新增 .js
   **替代方案**：维持 JS 双写法 —— 放弃，与技术栈确认矛盾
2. **决策**：anti-pattern-rules.md 全部 20 条规则的合规写法改为 TS（import/export、interface/type、zod、判别联合、as const、WeakSet、AbortSignal.timeout 等），适用范围标注「现状 JS 路径 → TS 目标路径」
   **原因**：规则是给未来 TS 代码参照的，示例必须以 TS 为准
3. **决策**：RULE.mdc §五 表格中 `ns.logger`/`config.js`/`secrets.js`/`@typedef` 等 JS 旧表述同步为 `lib/logger.ts`/`src/shared/constants.ts`/`src/shared/secrets.ts`/显式类型契约
   **原因**：规则表需与细则一致

### 变更摘要
- [修改] .codebuddy/rules/coding-standards/anti-pattern-rules.md — 全文 TS 化（头部声明 + 20 条合规写法）
- [修改] .codebuddy/rules/coding-standards/RULE.mdc — §5.1/5.2/5.3 中 R1/R4/R14/R15/R16 与 CheckList 表述改为 TS 目标
- [修改] .codebuddy/wiki/14-TypeScript改造方案.md — 头部声明技术栈已确认 TS（2026-08-05 更新）
- [修改] .codebuddy/memory/MEMORY.md — 追加技术栈确认条目

### 影响范围
- 消息协议变更？否；存储结构变更？否；权限变更？否；需数据迁移？否
- 后续所有新代码按 TS 编写；编码规范示例以 TS 为准

### 待办/遗留问题
- [ ] P2：RULE.mdc §一/§二 中 `src/lib/errors.ts`、`src/lib/logger.ts` 路径与当前 js/ 现状的对应关系在迁移时核对
- [ ] P2：wiki/01-13 各章仍以 JS 现状描述，待迁移完成后再同步为 TS

### 质量门
- lint：文档变更为主，无代码影响
