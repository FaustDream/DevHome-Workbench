---
description: 开发流程规范（Chrome 扩展 MV3 · TypeScript）。从需求到发布的全链路流程：方案设计、代码实现、自测检查、提交 checklist、反模式红线清单、发布流程。开发新功能、修复 Bug、重构、准备提交或发布时必须遵守。
alwaysApply: false
enabled: true
---

# 开发流程规范（通用 Chrome 扩展 MV3 · TypeScript）

> 本规范定义从接收需求到发布上线的**全链路开发流程**，结合编码规范（`.codebuddy/rules/coding-standards/`）、开发结构（`.codebuddy/rules/architecture/`）、全局规则（`.codebuddy/rules/global/`）以及配套技能沉淀。具体约束以被引用规则为准，**本文件不重复罗列**。

---

## 1. 总则

| 原则 | 描述 |
| --- | --- |
| **文档先行** | 中大型功能必须先产出设计文档/Issue，AI 协助时应先阅读相关规范 |
| **规范先于代码** | 所有代码必须符合 `coding-standards`、`architecture` 规则 |
| **类型安全** | 全面使用 TypeScript（开启 `strict`），跨进程消息必须使用判别联合 |
| **零运行时样式** | UI 样式用 vanilla-extract，编译为静态 CSS |
| **最小权限** | 遵循 MV3 最小权限原则，权限变更需评估安全影响 |
| **测试守护** | 逻辑层（`lib/`）必须有单元测试，消息协议必须有穷尽性测试 |

---

## 2. 开发前准备

### 2.1 环境初始化

首次参与项目时完成：克隆仓库 → `npm install` → 验证 `node >= 18` → 在 Chrome 开发者模式加载已解压扩展。

### 2.2 需要阅读的规范（按优先级）

| 优先级 | 规范 | 适用场景 |
| --- | --- | --- |
| P0 | `.codebuddy/rules/coding-standards/` | 每行代码都必须遵守（常驻加载） |
| P0 | `.codebuddy/rules/architecture/` | 任何模块新增/重构 |
| P0 | `.codebuddy/rules/global/`（全局规则） | 所有开发（自动加载） |
| P1 | `.codebuddy/skills/chrome-ext-ui-design-system/SKILL.md` | UI 页面开发 |
| P1 | `.codebuddy/skills/typescript-advanced-types/SKILL.md` | 复杂类型逻辑 |
| P2 | `.codebuddy/skills/clerk-chrome-extension-patterns/SKILL.md` | 鉴权相关开发 |

### 2.3 加载对应 Skill

| 任务类型 | 需加载的 Skill |
| --- | --- |
| **写代码/开发功能/修复 Bug（全流程，推荐）** | **`coding-standard-workflow`** — 自动编排全流程 |
| 新增/修改 UI 页面、样式组件 | `chrome-ext-ui-design-system` |
| 实现复杂类型逻辑、类型安全协议 | `typescript-advanced-types` |
| 实现鉴权/登录/用户状态 | `clerk-chrome-extension-patterns` |

---

## 3. 任务生命周期

```
需求/Issue → 设计/方案 → 代码实现 → 自测/类型检查 → 提交/审查 → 上下文记忆 → 合并 → 发布
```

### 3.1 需求分析阶段

- **阅读现有代码**：确认需求是否已有部分实现，避免重复工作。
- **确认规范**：涉及 UI 引用技能 `chrome-ext-ui-design-system`，涉及消息协议引用 `architecture` 规则第 3 节。
- **产出**：对于中大型功能，在 `.codebuddy/memory/` 当日日志中记录变更方案与设计决策。

### 3.2 方案设计阶段

**小型改动**（单文件、不涉及跨上下文通信）：直接在代码实现阶段完成，无需单独方案文档。

**中大型改动**（涉及多个模块、跨进程通信、存储结构变更）：
- 按 `architecture` 规则的分层架构设计。
- 消息协议的新增/修改必须提前在 `src/shared/messages.ts` 设计中体现。
- 存储结构变更需设计迁移方案。
- 涉及 UI 的页面骨架参考 `chrome-ext-ui-design-system/templates/`。

**跨上下文边界必须校验的 checklist**（详见 `architecture` 规则第 3/8 节）：
- [ ] 消息体是否在 `src/shared/messages.ts` 中定义了判别联合？
- [ ] 是否在接收侧写了 switch 穷尽校验（`never` 兜底）？
- [ ] `payload` 是否只含可序列化数据？
- [ ] 是否在消息边界做了运行时类型守卫（`zod` `safeParse`）？
- [ ] 是否遵循 Branded Type（`SessionId`、`FilePath` 等）？
- [ ] `Result<T, E>` 是否处理了 `ok: false` 分支？

### 3.3 代码实现阶段

严格遵循 `coding-standards` 规则与 `architecture` 规则，**不在此复述其条款**。关键执行点：
- **模块边界红线**（不可违反）：`shared/` 与 `lib/` 无 `chrome.*`；`injected/` 是纯函数；Popup 不直连 Native Host；Background 状态用 `chrome.storage` 持久化。
- **编码规范**：禁止魔法值、外部输入必校验、统一错误体系、统一日志入口、类型安全（`unknown` 优先）。
- **UI 开发**：按 `chrome-ext-ui-design-system/SKILL.md` 标准步骤（Token → 复用模式 → `vars.*` → 内联 SVG → `prefers-reduced-motion`）。
- **消息通信**：按 `architecture` 第 3 节（判别联合 + `never` 兜底 + `zod` 守卫）。

> 目录结构、编码规范全文、消息通信完整代码均见对应规则/技能，勿在此复制。

### 3.4 自测与检查阶段

每个改动完成后必须执行对应检查（不通过不得提交）：
- **语法/类型检查**：`tsc --noEmit`
- **运行单元测试**：`npm test`
- **Lint 检查**：`eslint`

### 3.5 提交规范

#### 3.5.1 提交前 checklist

- [ ] 代码通过语法/类型检查
- [ ] 测试通过（单元测试）
- [ ] 无新增 lint 警告/错误
- [ ] 未违反模块边界红线（`lib/` 无 `chrome.*`、`shared/` 无 `chrome.*` 等）
- [ ] 所有外部输入已做空值/类型校验
- [ ] 消息协议如有变更，已更新 `src/shared/messages.ts` 并添加类型守卫
- [ ] 存储结构如有变更，已设计迁移方案
- [ ] UI 样式引用 `vars.*` Token，未写死字面量
- [ ] 未引入运行时 CSS 方案（仅 vanilla-extract 编译期）
- [ ] 无 `console.*` 残留（使用了统一日志入口）
- [ ] 无直接 `throw` 非业务异常
- [ ] 已在 `.codebuddy/memory/` 当日日志中记录记忆条目（见 `context-memory` 规则）

#### 3.5.2 Git 提交信息格式

```
<type>(<scope>): <简短描述>

<可选的详细描述>
<可选的关联 Issue>
```

| type | 含义 |
| --- | --- |
| feat | 新功能 |
| fix | Bug 修复 |
| refactor | 重构 |
| style | 样式/UI 调整 |
| test | 测试相关 |
| docs | 文档更新 |
| chore | 构建/工具/配置变更 |
| perf | 性能优化 |

### 3.6 审查与合并

- 提交前确保与主分支无冲突。
- 遵循最小化 diff、单一职责（一个提交只做一件事）。
- 涉及重大架构变更或消息协议变更，需额外审查。

---

## 4. 发布流程

### 4.1 版本号管理

三段式版本号：`MAJOR.MINOR.PATCH`

| 增量 | 场景 |
| --- | --- |
| MAJOR | 不兼容的 API/协议变更 |
| MINOR | 向下兼容的新功能 |
| PATCH | 向下兼容的 Bug 修复 |

版本号记录在 `manifest.json` 中的 `version` 字段。

### 4.2 发布步骤

1. **更新版本号**：`manifest.json` 中的 `version`。
2. **更新发布说明**：在 `CHANGELOG.md` 或发布说明模块记录变更。
3. **提交版本变更**：git commit（type 为 `chore`，scope 为 `release`）。
4. **打 Tag**：`git tag v<version>`。
5. **提交/推送**：`git push --tags`。
6. **Chrome 商店发布**（可选）：上传打包后的 `.zip` 到 Chrome 开发者控制台。

---

## 5. 典型开发场景（速览）

- **新增 UI 页面**：加载 `chrome-ext-ui-design-system` → 参考 templates 骨架 → 在 Token 范围内完成样式 → 如需扩展消息协议见 `architecture` 第 3 节 → 为 `lib/` 纯逻辑写单元测试。
- **新增跨上下文功能**：加载 `typescript-advanced-types` → `shared/types.ts` 定义品牌类型 → `shared/messages.ts` 扩展判别联合 → `lib/services/` 实现纯逻辑 → `background/` 实现路由与状态 → `pages/` 对接 UI → 所有边界加 `zod` 校验 → 消息穷尽性测试 + 逻辑单测。
- **Bug 修复**：确认所属层级（Popup/Background/Injected/Native Host）→ 修复 → 补充边界 case 测试 → 全量测试 → 提交（type: fix）。

---

## 6. 反模式红线清单

> **反模式红线为全局唯一权威源**，见 `rules/global/RULE.mdc` 第 5 节「模块边界红线」+ `coding-standards` 的编码红线 + `chrome-ext-ui-design-system` 的 UI 红线。**本文件不重复罗列**，避免多份维护不一致。AI 生成代码不得违反上述任一红线；跨上下文边界尤其注意：消息协议禁止 `any`、禁止跨进程传函数/DOM/Map/Set、密钥不得进前端包体。

---

## 7. 工具链快速参考

常用命令、推荐依赖、相关文件跳转见 `references/toolchain.md`（已外移，避免主规则膨胀）。

---

## 8. 流程速查图

```
需求/Issue → 方案设计(中大型) → 代码实现(遵循 coding-standards + architecture)
  → 自测(tsc + eslint + npm test) → 提交(checklist + Conventional Commit)
  → 上下文记忆(.codebuddy/memory/) → 合并 → 发布(版本+Tag)
```
