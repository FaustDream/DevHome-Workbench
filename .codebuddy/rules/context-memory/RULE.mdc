---
description: 上下文记忆工程规范。每次完成代码修改后，必须在 .codebuddy/memory/ 下记录结构化记忆条目（关键决策、变更范围、待办问题），并维护 MEMORY.md 长期记忆。依托 CodeBuddy 自带记忆系统实现。
alwaysApply: true
enabled: true
---

# 上下文记忆工程（依托 CodeBuddy 记忆系统）

> 每次完成**完整的修改任务**后，AI 必须在 `.codebuddy/memory/` 下记录结构化记忆条目，以便后续开发快速回溯历史决策、变更范围与遗留问题。

## 1. 存储位置

| 文件 | 用途 | 更新方式 |
| --- | --- | --- |
| `.codebuddy/memory/YYYY-MM-DD.md` | 当日开发日志（每个任务一节） | **追加**，不覆盖 |
| `.codebuddy/memory/MEMORY.md` | 长期记忆（技术栈、关键架构决策、长期待办） | **原地更新**，保持精炼 |

- 当日文件不存在时先创建。
- 项目级关键决策完成后同步蒸馏进 `MEMORY.md`（避免只留在日志里被淹没）。

## 2. 记忆条目模板（追加到当日日志）

```markdown
## HH:mm - <任务标题>（<feat|fix|refactor|chore|docs|test|style|perf>）

### 任务描述
简要说明做了什么、为什么做。

### 关键决策
1. **决策**：选择的方案
   **原因**：为什么这样选
   **替代方案**：考虑过但放弃的方案及原因

### 变更摘要
- [新增/修改/删除] path — 说明

### 影响范围
- 消息协议变更？是/否；存储结构变更？是/否；权限变更？是/否；需数据迁移？是/否

### 待办/遗留问题
- [ ] 问题（P0/P1/P2）

### 质量门
- tsc / vitest / build 结果
```

## 3. 执行时机

**必须记录**：新增功能、修复 Bug、重构、消息协议变更、存储结构变更、目录结构变更、构建/配置变更、依赖更新。
**不需单独记录**：仅拼写/格式修复、仅注释变更、单文件微调。

**写入时机（会话结束统一落盘，最大化缓存命中率）**：
- **会话内仅累积**：单次任务（含连续小改动）完成后**不立即写文件**，仅在本轮回复中简述要点；要点暂存于上下文，待本会话结束一次性追加到当日日志。
- **会话结束判定**：出现以下任一即触发统一落盘——(a) 用户明确表示"结束 / 完成 / 收工"；(b) 用户要求"记录 / 保存记忆"；(c) 本轮无待办且对话收尾。
- **不重复抄写已注入内容**：alwaysApply 规则、记忆摘要等已注入上下文，落盘时只记录本会话特有的决策、变更与待办。
- **关键决策仍即时记录**：仅当涉及跨会话不可丢失的架构 / 协议 / 存储结构变更（STEP 4 中大型改动）时，可在会话内即时追加；其余任务流水一律会话末统一落盘。

执行顺序：修改完成且测试通过 → 会话结束统一落盘记忆条目 → git commit（记忆先于提交；落盘时机为会话末，但提交前务必确保已落盘）。

## 4. 回顾时机

| 场景 | 回顾方式 |
| --- | --- |
| 开始新任务 | 读 `MEMORY.md` + 最近 1-2 天日志 |
| 接手未完成任务 | 读对应日期日志 |
| 遇到回归 Bug | 在 `.codebuddy/memory/` 内搜索关键词 |
| 修改既有功能 | 读该功能相关记忆，了解原始设计决策 |

## 5. 反模式（避免）

- ❌ 记录过于简短（一两句话）、不记录替代方案、不记录待办
- ❌ 记录时机滞后（隔天再补）
- ❌ 把敏感信息（密钥、令牌）写入记忆
- ❌ 用记忆更新替代正常回复或用户要求的交付物
