# DevHome Workbench 代码审查规范

> **版本**: v1.0.0 | **生效日期**: 2026-07-17 | **适用范围**: DevHome Workbench 项目全部源码及构建产物
>
> 本规范是 AGENTS.md 的补充，专门定义代码审查的流程、标准与执行细则。AGENTS.md 中的编码规范、注释要求、日志规范、弹窗规范等在本审查流程中作为**强制检查项**对待。

---

## 目录

1. [审查维度总览](#1-审查维度总览)
2. [触发时机](#2-触发时机)
3. [参与角色与职责](#3-参与角色与职责)
4. [详细检查清单](#4-详细检查清单)
   - [4.1 代码质量维度（CQ）](#41-代码质量维度cq)
   - [4.2 功能实现维度（FI）](#42-功能实现维度fi)
   - [4.3 界面样式维度（UI）](#43-界面样式维度ui)
5. [问题分级标准](#5-问题分级标准)
6. [完整流程规范](#6-完整流程规范)
   - [6.1 提交流程](#61-提交流程)
   - [6.2 审查执行流程](#62-审查执行流程)
   - [6.3 闭环反馈流程](#63-闭环反馈流程)
7. [特殊场景处理](#7-特殊场景处理)
8. [附录](#8-附录)

---

## 1. 审查维度总览

代码审查覆盖三大维度，每个维度下设若干子项：

```
审查维度
├── 代码质量（CQ - Code Quality）
│   ├── CQ-1 可读性    —— 命名、注释、结构、复杂度
│   ├── CQ-2 规范性    —— 编码约定、模块模式、文件组织
│   ├── CQ-3 性能      —— 渲染性能、存储 I/O、内存管理
│   ├── CQ-4 安全性    —— 数据安全、XSS 防护、权限最小化
│   └── CQ-5 错误处理  —— 异常捕获、降级策略、日志输出
│
├── 功能实现（FI - Functional Implementation）
│   ├── FI-1 需求符合度 —— 是否完成需求描述的所有功能点
│   ├── FI-2 逻辑正确性 —— 业务逻辑是否正确、状态流转是否合理
│   └── FI-3 边界情况   —— 空数据、极端值、并发冲突、降级
│
└── 界面样式（UI - User Interface）
    ├── UI-1 一致性    —— 视觉元素、间距、色彩是否遵循设计系统
    ├── UI-2 布局合理性 —— 组件排列、间距、层级关系
    ├── UI-3 响应式    —— 不同视口尺寸下的适配表现
    └── UI-4 交互体验  —— 反馈、动画、键盘可访问性
```

每个检查项有一个三位编号（维度缩写-序号），用于审查报告中精确引用。

---

## 2. 触发时机

| 触发条件 | 审查级别 | 说明 |
|----------|---------|------|
| **PR / 分支合并前** | **完整审查** | 任何从功能分支合并到 `main` 的操作，必须经过完整三维度审查 |
| **新功能开发完成** | **完整审查** | 新增模块、页面、组件，覆盖三维度全部检查项 |
| **Bug 修复** | **定向审查** | 重点关注 FI-2（逻辑正确性）、FI-3（边界）、CQ-5（错误处理），其余按需 |
| **重构** | **定向审查** | 重点 CQ-1（可读性）、CQ-2（规范性），需确认行为不变（回归） |
| **版本发布前** | **门禁审查** | 快速过检：构建通过 + 测试全绿 + P0 问题清零 |
| **hotfix 紧急修复** | **加速审查** | 仅检查 P0 安全/回归项，其余项目可后补 |
| **定期（每 2 周）** | **巡检审查** | 扫描技术债务（🔴文件、未处理 TODO、日志死循环风险） |

### 最小审查覆盖规则

- **单文件变更（<50 行）**：至少由 1 人审查，聚焦变更行及影响范围
- **单文件变更（≥50 行）或多文件变更**：必须由 1 人主审 + 自检清单
- **核心基础设施变更**（storageV2.js、state.js、config.js、build.mjs、background.js、fileConfig.js）：必须由 2 人审查，其中至少 1 人熟悉该模块
- **新增文件**：必须检查文件位置、命名、模块注册（build.mjs 中的 files 数组或 index.html 的 `<script>` 标签）

---

## 3. 参与角色与职责

| 角色 | 谁担任 | 职责 |
|------|--------|------|
| **作者（Author）** | 代码提交者 | 完成自检清单、编写清晰的提交说明、响应审查意见 |
| **审查人（Reviewer）** | 项目成员（至少 1 人） | 对照检查清单逐项审查、提出问题、确认修复 |
| **门禁（Gatekeeper）** | 项目负责人 / 指定维护者 | 最终批准合并、确认 P0 清零、版本号更新 |
| **自动化（CI Bot）** | 构建脚本 + 测试套件 | 自动检查：构建通过、测试全绿、无 console 残留（prod 模式） |

### 本项目角色映射

由于当前项目为个人维护，审查人可通过以下方式补位：

- **自我审查模式**：提交者在合并前，必须完成自检清单并间隔 ≥2 小时后重新审视（"冷静期"原则）
- **AI 辅助审查**：使用本规范的检查清单，由 AI 工具逐项生成审查报告
- **关键模块强制双审**：核心基础设施变更建议邀请外部审查者

---

## 4. 详细检查清单

### 4.1 代码质量维度（CQ）

#### CQ-1 可读性

| 编号 | 检查项 | 严重级别 | 判定标准 |
|------|--------|---------|---------|
| CQ-1.1 | 命名语义化 | P1 | 变量/函数名是否准确反映业务含义？无 `data`/`temp`/`obj` 等无意义命名。IIFE 模块命名空间下方法名是否与业务一致？ |
| CQ-1.2 | 中文注释完整性 | P1 | 对照 AGENTS.md §5.2 的 7 条注释强制要求：方法业务用途、关键变量含义、核心业务逻辑、状态流转原因、外部调用原因、字段含义是否都有注释？ |
| CQ-1.3 | 禁止坏注释 | P2 | 是否存在空洞翻译注释（如 `// set name`）、逐行无意义注释、用注释掩盖混乱代码？ |
| CQ-1.4 | 函数职责单一 | P1 | 单个函数是否只做一件事？是否存在 UI 渲染 + 网络请求 + 数据转换混杂在同一函数中？参考 AGENTS.md §9 行数标准。 |
| CQ-1.5 | 条件嵌套深度 | P2 | `if/for/while` 嵌套是否超过 3 层？复杂条件是否提取为命名良好的布尔函数？ |
| CQ-1.6 | 魔法数字 | P2 | 是否使用了裸数字字面量？应提取为命名常量或引用 config.js 中的配置。 |
| CQ-1.7 | 文件体积 | P1/P0 | 对照 AGENTS.md §9 的行数阶梯：🟢 <300 正常；🟡 300-500 需关注；🟠 500-800 需说明理由；🔴 >800 必须拆分 |

#### CQ-2 规范性

| 编号 | 检查项 | 严重级别 | 判定标准 |
|------|--------|---------|---------|
| CQ-2.1 | IIFE 模块模式 | P1 | 新增 JS 文件是否遵循 `window.DevHome = window.DevHome \|\| {}; (function(ns){ 'use strict'; ... })(window.DevHome);` 模式？命名空间挂载是否正确？ |
| CQ-2.2 | 文件加载顺序 | P0 | 新增文件是否在 `build.mjs` 的 `businessFiles` 数组中按依赖顺序注册？是否在 index.html（非打包模式时）正确引入？ |
| CQ-2.3 | 编码规范 | P0 | 文件编码是否 UTF-8？缩进风格是否与项目一致（2空格）？行尾是否使用 LF？ |
| CQ-2.4 | 全局命名空间隔离 | P0 | 是否向 `window.DevHome` 添加了合理命名的 API？是否污染了全局作用域？ |
| CQ-2.5 | 禁止原生弹窗 | P0 | 参照 AGENTS.md §10：是否使用了 `alert()`/`confirm()`/`prompt()`？必须使用 `ns.showConfirm`/`ns.showToast`/`ns.showPrompt` |
| CQ-2.6 | 交互日志规范 | P1 | 参照 AGENTS.md §11：点击/面板/编辑/警告/错误/模式切换是否输出结构化日志？高频事件（scroll/resize/mousemove）是否做了节流？ |
| CQ-2.7 | 第三方依赖克制 | P2 | 是否为了一个小功能新增了 npm 包？优先使用项目已有依赖或原生 API。新增依赖必须记录原因。 |
| CQ-2.8 | Shadcn 组件规范 | P1 | 参照 AGENTS.md §12：新增 Shadcn 组件是否在 `js/components/ui/`、正确替换 CSS 变量映射、暴露到全局 `window.ShadcnXxx`、成功编译？ |

#### CQ-3 性能

| 编号 | 检查项 | 严重级别 | 判定标准 |
|------|--------|---------|---------|
| CQ-3.1 | DOM 操作效率 | P1 | 是否存在循环内操作 DOM？是否使用批量更新或 DocumentFragment？是否缓存了 DOM 查询结果？ |
| CQ-3.2 | 事件监听泄漏 | P0 | 是否在组件销毁/页面切换时正确解绑事件？是否存在未清理的 `addEventListener`（尤其注意 `chrome.storage.onChanged`）？ |
| CQ-3.3 | 存储 I/O 优化 | P1 | chrome.storage.local 调用是否必要？是否合理使用了 localStorage 缓存层（storageV2.js 的 24h TTL 缓存）？是否避免在循环中多次读写存储？ |
| CQ-3.4 | 防抖/节流 | P1 | 搜索输入、resize 响应、自动保存等高频操作是否做了防抖（推荐 300-800ms）？ |
| CQ-3.5 | 内存泄漏风险 | P1 | 是否存在未清理的定时器（setInterval/setTimeout）？大型对象引用是否在不需要时释放？ |
| CQ-3.6 | 首屏加载 | P2 | 新增功能是否显著增加首屏加载时间？是否可以考虑懒加载？ |
| CQ-3.7 | 构建产物大小 | P2 | 新增代码是否导致 bundle.js 或 CSS 体积显著膨胀（>10% 增量需说明）？ |

#### CQ-4 安全性

| 编号 | 检查项 | 严重级别 | 判定标准 |
|------|--------|---------|---------|
| CQ-4.1 | XSS 防护 | P0 | 用户输入的内容在插入 DOM 前是否经过了 `escapeHtml()` 或 `sanitizeHtml()`（来自 utils.js）？是否不当使用了 `innerHTML` 或 `dangerouslySetInnerHTML`？ |
| CQ-4.2 | 敏感信息保护 | P0 | API Key/Token 是否仅在 `js/secrets.js` 中定义？（该文件在 .gitignore 中）是否符合 config.js 中的密钥引用模式？ |
| CQ-4.3 | 存储安全 | P1 | chrome.storage.local 中是否存储了敏感数据？localStorage 中是否有不应该持久化的敏感信息？ |
| CQ-4.4 | 外部资源加载 | P1 | 是否加载了不受信任的外部脚本/样式？URL 是否可被用户输入控制（潜在 XSS）？ |
| CQ-4.5 | Manifest 权限 | P0 | 是否新增了 Chrome Extension 权限？新权限必须有明确的业务理由和安全评估。 |
| CQ-4.6 | CSP 合规 | P2 | 内联脚本/样式是否与 manifest 中配置的 CSP 兼容？ |

#### CQ-5 错误处理

| 编号 | 检查项 | 严重级别 | 判定标准 |
|------|--------|---------|---------|
| CQ-5.1 | try-catch 覆盖 | P0 | 所有 async/await 操作、chrome.storage API 调用、fetch 请求是否有 try-catch？ |
| CQ-5.2 | 降级策略 | P1 | 当 chrome.storage.local 不可用时（如在非扩展环境测试），是否有 localStorage 降级路径？ |
| CQ-5.3 | 错误日志 | P1 | catch 块中是否使用 `console.error('[错误] ...', err)` 输出了结构化日志？是否使用了 logger.js 的分级日志？ |
| CQ-5.4 | 用户友好提示 | P1 | 关键操作失败时是否通过 toast 或内联提示告知用户？错误信息是否用户可理解（而非直接暴露技术堆栈）？ |
| CQ-5.5 | 失败重试 | P2 | 网络请求失败是否有合理的重试机制？storageV2.js 的乐观锁是否使用了 3 次重试？ |
| CQ-5.6 | 空状态/加载态 | P1 | 异步数据加载是否处理了 loading、empty、error 三种状态？参照 AGENTS.md 中"生产级而非 demo 级"原则。 |

---

### 4.2 功能实现维度（FI）

#### FI-1 需求符合度

| 编号 | 检查项 | 严重级别 | 判定标准 |
|------|--------|---------|---------|
| FI-1.1 | 功能点完整 | P0 | 需求描述中的所有功能点是否全部实现？是否有遗漏？ |
| FI-1.2 | 验收标准匹配 | P0 | 是否有对应的验收标准（如"用户可以..."）？每条标准是否满足？ |
| FI-1.3 | 非功能性需求 | P1 | 需求中的隐含要求是否满足：加载速度、数据持久化、跨标签页同步（如适用）？ |
| FI-1.4 | 需求理解偏差 | P0 | 实现逻辑是否与需求意图一致？是否存在理解偏差导致的错误实现？ |

#### FI-2 逻辑正确性

| 编号 | 检查项 | 严重级别 | 判定标准 |
|------|--------|---------|---------|
| FI-2.1 | 核心路径正确 | P0 | 主流程（happy path）是否执行正确？关键状态转换是否符合预期？ |
| FI-2.2 | 状态管理一致 | P0 | 数据变更后，所有相关 UI 是否同步更新？是否存在状态与视图不一致？ |
| FI-2.3 | 数据流清晰 | P1 | 数据流向是否清晰（单向数据流）？是否存在循环依赖或竞态条件？ |
| FI-2.4 | 条件分支覆盖 | P1 | 所有 `if/else/switch` 分支是否经过验证？是否有不可达代码？ |
| FI-2.5 | 异步操作顺序 | P0 | 多个异步操作的执行顺序是否正确？是否存在未等待完成的 Promise 导致数据不完整？ |
| FI-2.6 | 存储一致性 | P0 | chrome.storage.local 和 localStorage 缓存是否一致？乐观锁冲突是否正确处理？ |

#### FI-3 边界情况

| 编号 | 检查项 | 严重级别 | 判定标准 |
|------|--------|---------|---------|
| FI-3.1 | 空/null/undefined 处理 | P0 | 输入为空字符串、null、undefined、空数组、空对象时是否不崩溃且有合理行为？ |
| FI-3.2 | 极端值处理 | P1 | 数值为 0、负数、极大值、浮点精度时是否行为正确？ |
| FI-3.3 | 并发操作 | P1 | 用户快速连续点击、多次提交、同时操作多个标签页时是否不会产生数据损坏？ |
| FI-3.4 | 存储配额耗尽 | P1 | chrome.storage.local 达到配额上限（storageV2.js 已定义 90% 阈值）时是否有告警策略？ |
| FI-3.5 | 离线/网络异常 | P1 | 天气 API、AI API、格言 API 等外部请求失败时，是否有降级显示（如"天气信息暂不可用"）？ |
| FI-3.6 | 旧数据兼容 | P1 | 数据迁移（localStorage → chrome.storage.local）是否正确处理了旧版数据格式？File System Access API 权限衰减后是否优雅降级？ |
| FI-3.7 | 扩展上下文失效 | P2 | Service Worker 被浏览器回收后，background.js 的状态是否能正确恢复？ |

---

### 4.3 界面样式维度（UI）

#### UI-1 一致性

| 编号 | 检查项 | 严重级别 | 判定标准 |
|------|--------|---------|---------|
| UI-1.1 | CSS 变量使用 | P0 | 是否使用项目定义的 Semantic Token CSS 变量（`--color-bg`、`--color-text`、`--color-accent`、`--color-border` 等）？禁止硬编码颜色值。 |
| UI-1.2 | 主题适配 | P0 | 新增 UI 是否在浅色（default）和深色（data-color-scheme="dark"）两种模式下都表现正常？拟物化（skeuomorphic）和极简（minimal）主题是否兼容？ |
| UI-1.3 | 字体/排版 | P1 | 是否使用 Inter 可变字体？字号、行高、字重是否与项目设计系统一致？ |
| UI-1.4 | 圆角/阴影/间距 | P1 | 卡片圆角、阴影层级、间距倍率是否遵循项目现有的视觉节奏？ |
| UI-1.5 | 禁止原生弹窗 | P0 | 是否存在原生 alert/confirm/prompt 破坏视觉一致性？ |

#### UI-2 布局合理性

| 编号 | 检查项 | 严重级别 | 判定标准 |
|------|--------|---------|---------|
| UI-2.1 | 组件层级关系 | P1 | z-index 层级是否清晰？弹窗/下拉/右键菜单是否正确覆盖？有无遮挡问题？ |
| UI-2.2 | 溢出处理 | P1 | 长文本、多列表格、大图片等溢出场景是否使用了截断（ellipsis）、滚动或换行？ |
| UI-2.3 | 弹窗/面板定位 | P1 | 磁贴编辑弹窗、AI 聊天面板、设置面板等是否在视口内居中且不超出边界？ |
| UI-2.4 | 多入口一致性 | P1 | 同一功能在 index.html（新标签页）、popup.html（弹窗）、sidepanel.html（侧边栏）中的 UI 是否一致？ |

#### UI-3 响应式适配

| 编号 | 检查项 | 严重级别 | 判定标准 |
|------|--------|---------|---------|
| UI-3.1 | 视口适配 | P0 | 在 1920px（全屏桌面）、1024px（平板）、380px（popup）三种视口下布局是否可用且美观？ |
| UI-3.2 | 磁贴网格自适应 | P1 | 磁贴网格列数是否根据容器宽度自适应？F5 布局预设切换是否正常？ |
| UI-3.3 | 触摸/移动端 | P2 | 在平板触摸操作下，点击目标是否 ≥ 44x44px？拖拽排序是否可用？ |
| UI-3.4 | 侧边栏适配 | P2 | sidepanel 在 Chrome 侧边栏（默认 320-400px 宽）中布局是否完整？ |

#### UI-4 交互体验

| 编号 | 检查项 | 严重级别 | 判定标准 |
|------|--------|---------|---------|
| UI-4.1 | 操作反馈 | P1 | 按钮点击、表单提交、数据保存是否有即时反馈（hover 状态、active 状态、loading 状态、toast 通知）？ |
| UI-4.2 | 过渡动画 | P2 | 面板打开/关闭、页面切换、弹窗出现是否有平滑过渡？（参照 animations.css）是否遵循 `prefers-reduced-motion` ？ |
| UI-4.3 | 键盘可访问性 | P1 | Tab 键能否按逻辑顺序导航？Enter/Esc 是否在弹窗中正常工作？专注模式快捷键是否不受影响？ |
| UI-4.4 | 焦点管理 | P1 | 弹窗打开后焦点是否自动落入合理元素？弹窗关闭后焦点是否回到触发元素？ |
| UI-4.5 | 右键菜单 | P1 | 磁贴右键菜单、空白区域右键菜单是否在正确位置出现？菜单项是否符合预期？ |
| UI-4.6 | 拖拽体验 | P2 | 磁贴拖拽排序是否有视觉反馈（placeholder、drag 镜像）？拖拽到非法区域时是否回弹？ |

---

## 5. 问题分级标准

所有审查发现的问题按紧急程度分为四个等级：

| 级别 | 名称 | 定义 | 处理要求 | 示例 |
|------|------|------|---------|------|
| **P0** | 🔴 阻断 | 导致功能崩溃、数据丢失、安全漏洞、构建失败、不符合强制性规范 | **必须修复，禁止合并** | XSS 漏洞、存储数据损坏、未捕获异常导致白屏、build 失败、权限泄漏、核心功能逻辑错误 |
| **P1** | 🟠 严重 | 影响用户体验、违反编码规范、存在潜在风险、边界情况未处理 | **本次合并前必须修复** | 缺少错误处理、缺少注释、日志不规范、UI 主题不适配、缺少空状态、DOM 事件泄漏 |
| **P2** | 🟡 改进 | 代码优雅度、性能优化空间、更好的实现方式 | **创建 Issue 跟踪，可在后续迭代修复** | 命名可优化、函数可提取、可添加过渡动画、可优化首屏加载 |
| **P3** | 🔵 建议 | 非强制性的优化建议、个人偏好、未来可考虑方向 | **可选采纳** | "这个变量名可以更短"、"可以考虑用 switch 替代 if-else" |

### 合并门禁标准

| 场景 | 门禁条件 |
|------|---------|
| 普通 PR | P0 = 0，P1 ≤ 3（需有修复计划） |
| 核心基础设施 PR | P0 = 0，P1 = 0 |
| hotfix | P0 = 0，P1 可延后但需建 Issue |
| 版本发布 | P0 = 0，P1 = 0，P2 已建 Issue 跟踪 |

---

## 6. 完整流程规范

### 6.1 提交流程

```
作者准备阶段
├── 1. 完成自检清单（见附录 B）
│   ├── 逐项对照 §4 的检查清单自审
│   ├── 运行 npm run build:components 确认构建通过
│   ├── 运行 npm run test:all 确认测试全绿
│   └── 在 Chrome 中加载扩展实测功能
│
├── 2. 编写提交说明
│   ├── 标题：[类型] 简要描述（类型：feat/fix/refactor/style/perf/test/docs）
│   ├── 正文：
│   │   - 为什么做这个改动（问题和动机）
│   │   - 做了什么（改动摘要，列出受影响文件）
│   │   - 如何验证（测试结果、手动测试步骤）
│   │   - 是否有 Breaking Change
│   │   - 关联 Issue 编号
│   └── 版本号更新：参照 AGENTS.md 版本规则更新 manifest.json 中的 version
│
├── 3. 提交代码
│   ├── 单次提交包含完整的逻辑单元（不要混入无关改动）
│   ├── 确保 js/secrets.js 未出现在变更中
│   └── 推送到功能分支
│
└── 4. 发起审查请求
    ├── 指定审查人（个人项目进入自我审查模式）
    └── 标注审查级别（完整/定向/加速）
```

### 6.2 审查执行流程

```
审查阶段
├── 1. 环境准备
│   ├── 拉取分支代码
│   ├── npm install（如有依赖变更）
│   ├── npm run build:components 确认构建
│   └── npm run test:all 确认测试
│
├── 2. 变更概览
│   ├── 阅读提交说明，理解改动动机
│   ├── 查看文件变更列表，识别核心变更
│   └── 确认 File List 与提交说明一致
│
├── 3. 逐维度审查
│   ├── 第一轮：代码质量（约 40% 时间）
│   │   ├── 阅读核心变更文件
│   │   ├── 对照 CQ-1~CQ-5 子清单
│   │   └── 标记问题（编号 + 级别 + 文件:行号）
│   │
│   ├── 第二轮：功能实现（约 30% 时间）
│   │   ├── 理解业务逻辑
│   │   ├── 对照 FI-1~FI-3 子清单
│   │   ├── 在 Chrome 中手动验证关键路径
│   │   └── 标记问题
│   │
│   └── 第三轮：界面样式（约 30% 时间）
│       ├── 在多种视口/模式下检查
│       ├── 对照 UI-1~UI-4 子清单
│       └── 标记问题
│
├── 4. 输出审查报告（见附录 A 模板）
│   ├── 总览：审查文件数、发现问题数、门禁判定
│   ├── P0/P1/P2/P3 分类列表
│   ├── 总体评价
│   └── 合并建议（通过/通过需修复/拒绝）
│
└── 5. 审查意见提交
    ├── 在 PR 中逐条评论问题（附编号）
    └── 输出汇总报告至 PR 描述
```

### 6.3 闭环反馈流程

```
   ┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐
   │  Author  │     │ Reviewer │     │Gatekeeper│     │   CI     │
   │  提交代码  │────▶│  审查代码  │────▶│  门禁确认  │────▶│  合并发布  │
   └──────────┘     └──────────┘     └──────────┘     └──────────┘
        ▲                │                │                │
        │    提出问题     │                │                │
        └────────────────┘                │                │
        │        修复后重新提交             │                │
        └─────────────────────────────────┘                │
                                                           │
   修复循环（最多 3 轮）                                      │
   ├── Round 1: 作者修复所有 P0/P1 问题                       │
   │   ├── 每个修复在独立 commit 中（便于 re-review）          │
   │   └── 回复每个审查评论，标注已修复或用 @ 讨论             │
   │                                                         │
   ├── Round 2: 审查人 re-review 修复内容                      │
   │   ├── 确认 P0/P1 问题已修复                              │
   │   ├── 标记已解决的问题为 resolved                        │
   │   └── 如有新问题，继续下一轮                              │
   │                                                         │
   └── Round 3（最终）: 门禁确认                               │
       ├── 确认门禁标准达标                                    │
       ├── 确认版本号已更新                                    │
       └── 批准合并                                           │
                                                              │
   合并后                                                      │
   ├── 未修复的 P2 问题自动创建 Issue                           │
   ├── 重大变更更新 CHANGELOG（如存在）                         │
   └── 回顾总结：审查发现了什么模式性问题？需要更新 AGENTS.md 吗？  │
```

---

## 7. 特殊场景处理

### 7.1 大型重构审查

当变更涉及 ≥5 个文件或 ≥200 行时：
- 增加架构审查环节：检查文件拆分是否符合 AGENTS.md §9 的拆分规则（复用性+领域独立性+物理邻近原则）
- 要求作者提供重构前后对比说明
- 优先审查公共 API 变更（对外接口签名、存储格式变更）

### 7.2 存储层变更审查

涉及 storageV2.js、fileConfig.js、dataService.js 或存储格式变更时：
- **必须**验证数据迁移路径（旧格式 → 新格式）且不丢失数据
- **必须**验证乐观锁（_version CAS）在并发写入时不损坏数据
- **必须**手动测试：旧版数据升级后功能正常

### 7.3 UI 组件新增审查

新增 Shadcn/ui 组件或页面时：
- 检查 CSS 变量映射表是否正确替换（参照 AGENTS.md §12.6）
- 检查是否在 `js/components/ui/` 目录，且编译输出到 `js/ui-components/`
- 检查在浅色/深色/拟物/极简四种主题下的表现

### 7.4 Chrome Extension API 使用审查

涉及 chrome.* API 使用时：
- 确认 Service Worker（background.js）中使用的 API 是否与 MV3 兼容
- 检查 chrome.storage.onChanged 监听是否正确处理跨上下文同步
- 确认权限声明（manifest.json）与实际使用一致

### 7.5 自我审查模式

个人项目缺少第二个审查者时：
- **强制冷静期**：提交后至少间隔 2 小时再审查，从"作者思维"切换到"审查者思维"
- **必须**完成完整的自检清单（附录 B），不可跳过
- **必须**在 Chrome 中做至少一次完整功能走查（不只依赖自动化测试）
- 审查报告同样需要归档

---

## 8. 附录

### 附录 A：审查报告模板

```markdown
# 代码审查报告

**审查编号**: CR-YYYYMMDD-NNN
**审查日期**: YYYY-MM-DD
**分支**: feature/xxx → main
**审查人**: [姓名]
**审查级别**: [完整审查 / 定向审查 / 门禁审查 / 加速审查]

---

## 变更概要
- 文件数: X 个
- 新增行: +XXX
- 删除行: -XXX
- 改动说明: [一句话概括]

## 构建与测试
- npm run build:components: ✅ / ❌
- npm run test:all: ✅ / ❌ (通过率: XX%)
- 手动功能验证: ✅ / ❌ / 部分

## 问题汇总

| 级别 | 数量 | 处置 |
|------|------|------|
| P0 阻断 | N | 必须修复 |
| P1 严重 | N | 必须修复 |
| P2 改进 | N | 创建 Issue |
| P3 建议 | N | 可选 |

## 问题详情

### P0 阻断

| 编号 | 检查项 | 位置 | 描述 |
|------|--------|------|------|
| CQ-x.x | 检查项名 | file.js:L123 | 问题描述 |

### P1 严重

| 编号 | 检查项 | 位置 | 描述 |
|------|--------|------|------|
| ... | ... | ... | ... |

### P2 改进

| 编号 | 检查项 | 位置 | 描述 |
|------|--------|------|------|
| ... | ... | ... | ... |

### P3 建议

| 编号 | 检查项 | 位置 | 描述 |
|------|--------|------|------|
| ... | ... | ... | ... |

## 总体评价

[代码质量的总体印象、亮点、主要风险点]

## 合并建议

- [ ] 通过（无 P0/P1）
- [ ] 通过需修复（P0=0, P1≤3，承诺修复）
- [ ] 拒绝（P0>0，需重新提交）
```

### 附录 B：作者自检清单

提交前逐项确认：

```
□ 构建通过: npm run build:components 无错误
□ 测试通过: npm run test:all 通过率 ≥ 目标
□ Chrome 实测: 在浏览器中完成一次完整功能走查
□ 编码规范: 遵循 IIFE 模块模式、命名语义化
□ 注释完整: 对照 AGENTS.md §5.2 完成中文注释
□ 日志规范: 对照 AGENTS.md §11 添加/更新交互日志
□ 禁止原生弹窗: 无 alert/confirm/prompt
□ XSS 防护: 用户输入经过 escapeHtml/sanitizeHtml
□ 错误处理: async 操作有 try-catch、有降级、有 toast 提示
□ 空状态: loading/empty/error 三种状态均已实现
□ 主题适配: 浅色/深色模式下 UI 正常
□ 敏感信息: js/secrets.js 未出现在变更中
□ 文件注册: 新增文件已在 build.mjs businessFiles 数组或 HTML 中注册
□ 版本号: manifest.json version 已按规则更新
□ 提交说明: 清晰描述动机、改动、验证方法
□ 无无关改动: 本次提交仅包含任务相关代码
```

### 附录 C：快速参考卡片

**最易遗漏的 5 个检查项**：
1. CQ-2.2：新增文件忘记在 build.mjs 中注册
2. CQ-4.1：innerHTML 赋值未经过 HTML 转义
3. FI-3.1：空数组/空对象/undefined 没有防御性处理
4. UI-1.2：只在浅色模式下测试，深色模式布局错乱
5. CQ-3.2：事件监听未在页面切换时解绑，造成内存泄漏

**Chrome 扩展特有检查**：
- background.js 中的 chrome.alarms 是否在不需要时清除
- chrome.storage.onChanged 监听是否在 Service Worker 重启后仍然绑定
- Manifest V3 中不允许使用 eval()，代码中是否规避
- popup.html（380px 宽度）下新增 UI 是否可用

---

> **维护说明**：本规范随项目演进持续更新。每次审查实践中发现新的模式性问题或盲区，应更新对应检查清单。重大修订需记录版本历史。
>
> **版本历史**：
> - v1.0.0 (2026-07-17)：初始版本，覆盖代码质量、功能实现、界面样式三维度完整审查规范。
