# 新标签页主题设计参考清单

> 调研日期：2026-08-12  
> 基于全网 Chrome 新标签页扩展、开源项目及设计趋势的综合分析

---

## 一、主流风格分类与模板分析

### 1. 极简主义风格 (Minimalist)

**代表作品**：Bonjourr、nightTab、MinimalNewTab、Renewed Tab、Material You NewTab

| 维度 | 特征描述 |
|------|---------|
| **配色方案** | 单色/低饱和度色系，深色背景为主（`#1a1a2e`、`#16213e`、`#0f0f0f`），文字采用 `#e0e0e0` 或 `#ffffff` 高对比度白；强调色仅用于关键交互元素（如 `#6c63ff`、`#00d4ff`） |
| **组件布局** | 居中单列垂直排列：问候语 → 时间 → 搜索栏 → 快速链接（可选）。上下留白充裕，无侧边栏 |
| **交互方式** | 鼠标悬停显示隐藏控件（如设置齿轮）；单击背景切换壁纸；支持 Focus Mode 一键隐藏所有小组件 |
| **字体选择** | Inter、SF Pro Display、JetBrains Mono（等宽字体用于时间显示） |
| **动画效果** | 极简淡入（fade-in 0.3s）、模糊背景过渡；避免过度动效 |

**核心设计理念**：
- "Less is More" — 每个像素都有存在理由
- 背景图 + 时间 + 搜索 = 最小可用集合
- 设置面板默认隐藏，按需展开
- 支持纯色/渐变/动态背景三种模式

---

### 2. 自然风光风格 (Nature)

**代表作品**：Momentum、Bonjourr、Nature New Tab、Horizon Tab

| 维度 | 特征描述 |
|------|---------|
| **配色方案** | 以 Unsplash 高质量自然摄影为背景，前景文字白色半透明（`rgba(255,255,255,0.9)`）；叠加 20-40% 暗色遮罩（`rgba(0,0,0,0.3)`）确保文字可读性 |
| **组件布局** | 居中布局：顶部问候语 + 时间，中部主焦点区（待办/名言），底部天气/温度；组件均使用 `backdrop-filter: blur(10px)` 毛玻璃效果 |
| **交互方式** | 每日自动切换背景图（可收藏/跳过）；焦点模式切换；位置信息自动获取天气 |
| **核心元素** | 日出日落渐变遮罩、毛玻璃信息卡片、每日励志名言、实时天气温度 |
| **特色功能** | 根据时段自动变换问候语（Good Morning / Afternoon / Evening）；根据地理位置推荐本地风景壁纸 |

**配色规律**：
- 前景文字：白色系（`#ffffff` 到 `rgba(255,255,255,0.7)`）
- 遮罩层：黑色 `rgba(0,0,0,0.2-0.5)`，根据壁纸亮度动态调整
- 强调色：取壁纸主色调的互补色或邻近色
- 信息卡片：`background: rgba(0,0,0,0.2); backdrop-filter: blur(12px); border: 1px solid rgba(255,255,255,0.1);`

---

### 3. 抽象艺术风格 (Abstract Art)

**代表作品**：Mue、Tabliss（GIPHY 动态背景）、Chrome 官方主题画廊抽象类

| 维度 | 特征描述 |
|------|---------|
| **配色方案** | 大胆的渐变色组合：`#ff6b6b → #feca57`、`#a29bfe → #6c5ce7`、`#00b894 → #00cec9`；支持 CSS `linear-gradient` / `radial-gradient` / `conic-gradient` |
| **组件布局** | 组件沿 Z 轴分层：背景层（渐变/几何图形）→ 遮罩层 → 内容层；支持不规则网格布局 |
| **交互方式** | 鼠标移动产生视差效果（parallax）；支持自定义 CSS 代码注入；几何图形随滚动/时间变化 |
| **视觉元素** | 几何形状（圆形、三角形、多边形）随机分布、噪点纹理叠加、色彩流动动画 |
| **设计参考** | Material You 动态取色（从壁纸提取主色调应用到 UI 组件）、Glassmorphism（毛玻璃）、Neumorphism（新拟态） |

**配色规律**：
- 使用 HSL 色彩空间，通过调整色相（H）和亮度（L）生成和谐配色
- 多色渐变遵循"60-30-10"法则：60% 主色、30% 辅色、10% 强调色
- 暗色模式下使用低饱和度 + 高明度差

---

### 4. 动态背景风格 (Dynamic / Animated Background)

**代表作品**：Tabliss（GIPHY 集成）、iTab（动态壁纸）、Nova Tab（AI 生成壁纸）、QUIK 起始页（视频背景）

| 维度 | 特征描述 |
|------|---------|
| **背景类型** | GIF 动图、MP4/WebM 视频循环播放、CSS 动画粒子、Canvas 生成的天气效果（雨/雪/星空）、Three.js 3D 场景 |
| **配色方案** | 动态适应——通过 Canvas 采样背景色并动态调整前景文字亮度和对比度 |
| **性能考量** | 视频背景需 `autoplay muted loop playsinline`；`will-change` 优化动画性能；低性能模式下自动降级为静态图 |
| **组件布局** | 内容区使用半透明遮罩隔离动态背景的视觉干扰；关键组件添加 `backdrop-filter` 提升可读性 |
| **交互方式** | 支持暂停/播放背景动画；视频背景音量默认为 0；提供静态壁纸作为高性能替代 |

---

### 5. Dashboard 仪表盘风格

**代表作品**：start.me、iTab、MONKNOW、Start Page HQ、Nova Tab

| 维度 | 特征描述 |
|------|---------|
| **配色方案** | 清爽白底（`#f5f6fa`）或暗色（`#1e1e2e`）；卡片 `background: #ffffff` 圆角 12-16px；彩色图标用于功能区分 |
| **组件布局** | 多列网格布局（2-4 列自适应）；卡片式小部件（Widget Card）；左侧/顶部导航分类栏；支持拖拽排列和调整大小 |
| **小部件类型** | 时钟、日历、天气、待办清单、RSS 阅读器、股票行情、系统监控、笔记、书签网格、快捷搜索、AI 助手入口 |
| **交互方式** | 拖拽排序（drag & drop）、右键上下文菜单、卡片折叠/展开、全屏编辑模式、数据云端同步 |
| **设计参考** | iOS 小组件设计语言、Notion 的块编辑器思想、macOS Dashboard |

**网格布局规律**：
```
标准 Dashbord 布局:
┌──────────────────────────────────────┐
│         搜索栏 (全宽)                │
├───────────┬──────────┬───────────────┤
│  时钟/天气 │  快捷书签  │  待办清单     │
│  (1x1)    │  (2x1)    │  (1x2)       │
├───────────┴──────────┼───────────────┤
│  RSS 资讯流 (2x1)     │  日历 (1x1)   │
├───────────────────────┴───────────────┤
│        常用网站 (全宽网格)            │
└──────────────────────────────────────┘
```

---

### 6. 图标网格风格 (Icon Grid)

**代表作品**：Infinity New Tab、iTab、Chrome 原生新标签页

| 维度 | 特征描述 |
|------|---------|
| **配色方案** | 白色/浅灰背景，图标使用网站品牌色或自定义纯色圆形背景；支持自定义图标颜色和形状 |
| **组件布局** | 响应式网格（4-10 列），每个图标为 80x80 到 120x120 的正方形区域；图标下方显示网站名称（单行截断） |
| **图标样式** | 圆形（`border-radius: 50%`）或圆角矩形（`border-radius: 16px`）；支持自定义上传图标图片；未加载 favicon 时显示首字母缩写 |
| **交互方式** | 单击打开网站、右键编辑/删除、长按拖拽排序、中键/Shift+单击在新标签页打开；支持文件夹嵌套分组 |
| **特色功能** | 图标文件夹收纳、跨设备云同步、自动获取网站 favicon、图标间距和大小可调 |

**图标网格设计参数**：
- 图标尺寸：默认 80px，可调范围 60-120px
- 图标间距：12-20px（gap）
- 标签字体：12-14px，行高 1.2
- 圆角：图标 50%（圆形）或 16px（圆角方形）
- 背景色：图标容器使用 `background: rgba(128,128,128,0.1)` 或品牌色

---

### 7. 功能聚焦风格 (Productivity-Focused)

**代表作品**：Momentum（Focus Mode）、Nova Tab（番茄钟）、Anori（组件化工作台）

| 维度 | 特征描述 |
|------|---------|
| **核心功能** | 每日目标设定、番茄钟计时器、待办清单、笔记、专注模式屏蔽所有干扰 |
| **配色方案** | 暖色调（#f4a261、#e76f51）营造舒适感；深色模式使用暖色系的暗色版本 |
| **组件布局** | 主焦点区占据视觉中心，次要功能折叠到边缘或隐藏 |
| **交互方式** | 每日首次打开引导设定今日目标；番茄钟浮动按钮；一键进入全屏专注模式 |

---

## 二、配色方案汇总

### 常用配色体系

| 风格类型 | 背景色 | 前景/文字 | 强调色（Accent） | 卡片背景 |
|---------|--------|----------|-----------------|---------|
| **纯黑极简** | `#000000` / `#0d0d0d` | `#ffffff` / `#e0e0e0` | `#6c63ff` / `#00d4ff` | `rgba(255,255,255,0.05)` |
| **深蓝暗色** | `#1a1a2e` / `#16213e` | `#eaeaea` | `#e94560` / `#0f3460` | `rgba(255,255,255,0.08)` |
| **暖色暗色** | `#2d2424` / `#3d2c2c` | `#f5e6d3` | `#c97b5d` / `#e8b478` | `rgba(255,255,255,0.06)` |
| **清爽浅色** | `#f5f6fa` / `#ffffff` | `#2d3436` / `#636e72` | `#0984e3` / `#6c5ce7` | `#ffffff` + `box-shadow` |
| **自然暖光** | `#fef9ef` / `#fdf6e3` | `#4a4a4a` | `#f4a261` / `#e9c46a` | `rgba(255,255,255,0.8)` |
| **渐变背景** | CSS Gradient | `#ffffff` + 半透明 | 取渐变主色 | `rgba(255,255,255,0.15)` + blur |

### Material You 动态取色方案

Chrome 148+ 和 Android 12+ 采用的 Material You 设计语言：
- 从壁纸中提取 **Source Color**（源色）
- 自动生成 5 个色调阶梯（Tonal Palette）：Primary、Secondary、Tertiary、Neutral、Neutral Variant
- 每个阶梯包含 0-100 的亮度级别（0 = 白，100 = 黑）
- UI 组件根据 Light/Dark 主题自动选择合适的色调级别

### 暗色模式最佳实践

1. **不要用纯黑**（`#000000`），使用 `#121212` 或 `#1a1a2e` 作为最深层背景
2. **层级通过 elevation 表达**：每升高一层，背景色亮度 +2-8%（`rgba(255,255,255,0.05) → 0.08 → 0.12`）
3. **文字对比度**：正文 ≥ 4.5:1，大号文字 ≥ 3:1（WCAG AA 标准）
4. **去饱和**：暗色模式下的鲜艳色彩应降低饱和度 20-50%，避免视觉疲劳

---

## 三、组件排列规律

### 通用布局模式

```
┌─────────────────────────────────────────┐
│  [设置] [主题切换] [更多]        右上角  │  ← 工具栏区
├─────────────────────────────────────────┤
│                                         │
│            08:42                        │  ← 时间显示（大字，居中）
│        2026年8月12日 星期三             │  ← 日期（小字，居中）
│     下午好，今天也要加油哦！             │  ← 问候语（居中）
│                                         │
│  ┌─────────────────────────────────┐    │
│  │  🔍  搜索或输入网址...    [🔍]  │    │  ← 搜索栏（居中，全宽60%）
│  └─────────────────────────────────┘    │
│                                         │
│  ┌──┐ ┌──┐ ┌──┐ ┌──┐ ┌──┐ ┌──┐       │  ← 常用网站网格（居中）
│  │  │ │  │ │  │ │  │ │  │ │  │       │
│  └──┘ └──┘ └──┘ └──┘ └──┘ └──┘       │
│  GitHub Gmail Docs YT   Map  Figma    │
│                                         │
├─────────────────────────────────────────┤
│  📰 今日资讯 · 天气 22°C · 待办 3项   │  ← 底部信息栏（可选）
└─────────────────────────────────────────┘
```

### 三种核心布局模式对比

| 布局模式 | 适用风格 | 结构描述 | 代表作品 |
|---------|---------|---------|---------|
| **居中单列** | 极简、自然风光 | 所有内容垂直居中单列排列，最大宽度 600-800px | Bonjourr、Momentum、nightTab |
| **网格多列** | Dashboard、图标网格 | 使用 CSS Grid 2-4 列自适应，每个单元格独立卡片 | iTab、start.me、MONKNOW |
| **混合布局** | 功能聚焦、Chrome 原生 | 搜索栏全宽居中 + 下方图标网格 + 右上角工具栏 | Chrome NTP、Infinity Tab |

### 磁贴/图标网格布局参数

- **响应式断点**：
  - `>= 1200px`：6-8 列
  - `>= 900px`：4-6 列
  - `>= 600px`：3-4 列
  - `< 600px`：2-3 列
- **网格间距**：`gap: 12px` 到 `24px`
- **容器内边距**：`padding: 24px` 到 `48px`
- **最大宽度**：`max-width: 1200px; margin: 0 auto;`

---

## 四、交互方式分析

### 1. 搜索栏交互

| 特性 | 实现方式 |
|------|---------|
| **搜索引擎切换** | 搜索栏左侧/下方显示搜索引擎图标，点击切换（Google/Bing/DuckDuckGo/Baidu 等） |
| **搜索建议** | 输入时实时显示下拉建议面板，支持键盘 ↑↓ 选择、Enter 确认、Esc 关闭 |
| **搜索历史** | 本地存储最近 10-20 条搜索记录，悬停显示删除按钮 |
| **焦点行为** | 新标签页打开时自动聚焦搜索栏（可配置开关）；按 `/` 快捷键聚焦 |
| **视觉反馈** | 聚焦时搜索框边框色变化 + 微阴影扩大；搜索建议面板带入场动画 |

### 2. 书签/快捷方式交互

| 特性 | 实现方式 |
|------|---------|
| **添加** | 拖拽 URL 到网格区域、点击 "+" 占位符、右键菜单"添加到此页" |
| **编辑** | 右键 → 编辑名称/URL/图标；双击名称进入编辑模式 |
| **删除** | 右键删除、拖拽到"删除区域"、悬停显示 × 按钮 |
| **排序** | 拖拽排序（HTML5 Drag & Drop API），保存排序结果到 storage |
| **文件夹** | 支持嵌套文件夹，点击展开子网格；显示文件夹内项目计数 |
| **导入** | 支持从浏览器书签栏批量导入（chrome.bookmarks API） |

### 3. 背景切换机制

| 机制 | 描述 |
|------|------|
| **定时切换** | 每日自动更换（可配置频率：每次打开/每小时/每天/每周） |
| **手动切换** | 悬停显示切换按钮；← → 箭头键切换；设置面板中预览选择 |
| **图片源** | Unsplash API / Bing 每日壁纸 / 本地上传 / 纯色/渐变 / GIPHY / AI 生成 |
| **收藏机制** | 喜欢某张壁纸可收藏/固定，不被自动切换替换 |
| **智能匹配** | 根据时段、天气、季节自动匹配壁纸类别 |
| **遮罩控制** | 滑动条调节遮罩透明度（0-80%），平衡壁纸可见度与文字可读性 |
| **模糊效果** | 背景模糊（`filter: blur(0-40px)`），减少视觉干扰 |

### 4. 通用交互模式

- **右键菜单**：使用浏览器原生 `contextMenus` API 或自建上下文菜单
- **快捷键**：`Ctrl+K` / `/` 聚焦搜索、`Ctrl+D` 添加书签、`?` 显示快捷键帮助
- **动画微交互**：磁贴 hover 放大 1.05x + 阴影提升；删除时缩小淡出；添加时缩放弹入
- **减少动画**：尊重 `prefers-reduced-motion` 媒体查询，全局禁用动画
- **无障碍**：支持 Tab 键导航、ARIA 标签、屏幕阅读器友好、键盘完全可操作

---

## 五、可借鉴的具体设计元素

### 1. 快捷书签样式参考

```
┌──────────────────────────────────────────────┐
│  样式A: 圆形图标 + 文字                      │
│  ┌──────┐  ┌──────┐  ┌──────┐              │
│  │  🏠  │  │  📧  │  │  ▶️  │              │
│  │ Home │  │ Gmail│  │  YT  │              │
│  └──────┘  └──────┘  └──────┘              │
│  适用：Infinity / Chrome 原生               │
├──────────────────────────────────────────────┤
│  样式B: 卡片式链接                           │
│  ┌─── GitHub ──────────────────────────┐    │
│  │  🐙  github.com                    │    │
│  │  Where the world builds software   │    │
│  └────────────────────────────────────┘    │
│  适用：Dashboard 风格 / iTab               │
├──────────────────────────────────────────────┤
│  样式C: 极简文字列表                         │
│  · GitHub                                  │
│  · Gmail                                   │
│  · YouTube                                 │
│  适用：极简风格 / nightTab                  │
└──────────────────────────────────────────────┘
```

### 2. 搜索栏设计参考

```
样式A: 居中放大搜索栏（Momentum / Bonjourr）
┌─────────────────────────────────────────────────┐
│                                                 │
│          ┌──────────────────────────┐           │
│          │  🔍 搜索网页...      [→] │           │
│          └──────────────────────────┘           │
│              [ Google ▼ ]                       │
│                                                 │
└─────────────────────────────────────────────────┘

样式B: 顶部通栏搜索（Chrome 原生 / Infinity）
┌─────────────────────────────────────────────────┐
│ ┌─────────────────────────────────────────────┐ │
│ │  [G] 搜索或输入网址                    [🔍] │ │
│ └─────────────────────────────────────────────┘ │
│                                                 │
│    ┌──┐ ┌──┐ ┌──┐ ┌──┐ ┌──┐ ┌──┐            │
└─────────────────────────────────────────────────┘

样式C: 悬浮搜索按钮（Material You / minimal）
┌─────────────────────────────────────────────────┐
│                   20:42                         │
│           点击开始搜索...                        │
│                   ( / )                         │
└─────────────────────────────────────────────────┘
```

### 3. 常用网站网格布局

**参考布局参数**（来自 nightTab / Infinity / iTab）：

```
桌面端（≥1024px）:
┌─────────────────────────────────────┐
│  8列网格，图标 72x72               │
│  gap: 16px, padding: 32px          │
└─────────────────────────────────────┘

平板端（768-1023px）:
┌───────────────────────────────┐
│  6列网格，图标 64x64         │
│  gap: 12px, padding: 24px    │
└───────────────────────────────┘

移动端（<768px）:
┌───────────────────────┐
│  4列网格，图标 56x56  │
│  gap: 8px, padding: 16px │
└───────────────────────┘
```

### 4. 主题/外观设置面板

参考 iTab / Tabliss 的设置面板设计：

```
┌── 设置 ──────────────────────── [×] ─┐
│                                      │
│  外观                                │
│  ┌─ 主题 ───────────────────────┐   │
│  │  ○ 浅色  ● 深色  ○ 跟随系统  │   │
│  └──────────────────────────────┘   │
│                                      │
│  ┌─ 壁纸 ───────────────────────┐   │
│  │  来源: [Unsplash ▼]          │   │
│  │  关键词: [自然风光        ]  │   │
│  │  切换频率: [每日 ▼]          │   │
│  │  遮罩深度: [━━━━━○────] 30% │   │
│  │  模糊度:   [━━○─────────] 5px│   │
│  └──────────────────────────────┘   │
│                                      │
│  ┌─ 布局 ───────────────────────┐   │
│  │  磁贴列数: [━━━━━○────] 6   │   │
│  │  磁贴大小: [━━━━○─────] M   │   │
│  │  显示搜索栏: [ON ●]          │   │
│  └──────────────────────────────┘   │
│                                      │
│  ┌─ 字体 ───────────────────────┐   │
│  │  字体族: [Inter ▼]          │   │
│  │  字号:   [━━━━○─────] 中    │   │
│  └──────────────────────────────┘   │
└──────────────────────────────────────┘
```

---

## 六、主流新标签页扩展特性矩阵

| 扩展名称 | 风格定位 | 核心特色 | 壁纸源 | 小部件 | 开源 | 云同步 |
|---------|---------|---------|--------|--------|------|--------|
| **Momentum** | 自然风光 + 专注 | 每日目标、焦点模式、名言 | 自有摄影库 | 天气、待办、名言 | ❌ | ✅ |
| **Tabliss** | 组件化极简 | 完全自定义、背景源多样 | Unsplash/GIPHY/本地/渐变 | 天气、时钟、名言、待办、NBA | ✅ | ❌ |
| **Infinity New Tab** | 图标网格 | 海量图标、云同步、壁纸库 | 自有高清壁纸库 | 天气、笔记、待办 | ❌ | ✅ |
| **iTab** | iOS 风格 Dashboard | 小组件式卡片、分类侧边栏 | 海量壁纸+动态壁纸 | 日历、天气、热搜、倒计时 | ❌ | ✅ |
| **nightTab** | 极简暗色 | 深色主题、CSS自定义、本地优先 | 纯色/渐变/自定义 | 书签网格、搜索、时钟 | ✅ | ❌ |
| **Bonjourr** | 极简自然 | 轻量级、按时间变换背景 | Unsplash（时段匹配） | 天气、名言、快速链接 | ✅ | ❌ |
| **start.me** | Dashboard | RSS、邮件、社交集成 | 自定义背景 | 丰富小部件生态 | ❌ | ✅ |
| **Mue** | 抽象艺术 | 开源、组件化、模块化 | 纯色/渐变/自定义 | 天气、名言、快速链接、待办 | ✅ | ❌ |
| **Nova Tab** | 全功能工作台 | AI 生成壁纸、番茄钟、日历 | 多源 + AI 生成 | 时钟、日历、番茄钟、笔记、天气 | ❌ | ✅ |
| **Horizon Tab** | 自然风光 | 精美壁纸、天气时钟、专注 | Unsplash + 自有库 | 天气、时钟、名言、焦点 | ❌ | ❌ |
| **Renewed Tab** | 极简可定制 | 号称比 Tabliss 更可定制 | Unsplash | 丰富自定义小部件 | ✅ | ❌ |
| **Aura Tab** | 高颜值开源 | 拖拽文件夹、WebDAV备份 | 自定义 | 书签管理、相册查看器 | ✅ | ✅(WebDAV) |
| **Anori** | 组件化工作台 | 8种不同布局、文件夹 | 自定义 | 天气、书签、待办、笔记 | ✅ | ❌ |
| **QUIK 起始页** | 中文社区 | 视频背景、API壁纸、纯色 | 图片/视频/API/纯色 | 搜索、书签、时间 | ✅ | ❌ |
| **Material You NewTab** | Android 风格 | 动态取色、Material Design3 | 壁纸取色 | 时间、问候、搜索、快捷 | ✅ | ❌ |

---

## 七、设计突破口与创新方向建议

基于当前 "Thrilled" 项目已有的架构（磁贴系统、多分类、壁纸管理、搜索建议、设置面板、主题切换等），以下是可以借鉴和创新的方向：

### 1. 可以借鉴的元素
- **Material You 动态取色**：从壁纸提取主色调，自动应用到 UI 组件（分类按钮、搜索栏焦点色等）
- **毛玻璃卡片效果**：`backdrop-filter: blur()` + 半透明背景，提升层次感
- **iTab 的分类侧边栏**：左侧图标导航栏切换分类，释放顶部空间
- **Momentum 的每日焦点**：开机/首次打开时引导设定今日目标
- **nightTab 的键盘导航**：完整的键盘快捷键体系（已部分实现）
- **Tabliss 的小部件市场**：允许社区贡献自定义小部件

### 2. 可以创新的方向
- **AI 驱动主题**：根据浏览历史习惯/时间段自动推荐壁纸和布局
- **网页剪藏集成**：利用已有的剪藏功能，在新标签页展示剪藏内容流
- **情境感知布局**：工作时段自动切换到生产力布局（待办+日历），休闲时段切换到娱乐布局（视频+社交快捷方式）
- **焦点渐变**：根据当前时间在时间显示区域渲染日出→正午→日落→深夜的渐变色带
- **数据可视化**：将浏览统计（今日访问次数、最常访问站点等）以优雅图表展示

---

## 八、设计原则总结

1. **内容优先**：背景和装饰服务于内容，不喧宾夺主
2. **即时可用**：打开新标签页 0 延迟（壁纸预加载、懒加载非关键组件）
3. **尊重隐私**：本地存储优先，云同步可选且透明
4. **个性化与默认值平衡**：提供精心设计的默认配置，同时保留完全自定义能力
5. **无障碍包容**：支持 `prefers-reduced-motion`、`prefers-color-scheme`、键盘全操作
6. **渐进式功能**：核心功能轻量快速，高级功能按需加载
7. **跨平台一致**：Chrome + Edge + Firefox 统一的视觉和交互体验

---

*本清单基于 2026 年 8 月全网调研整理，覆盖 15+ 主流新标签页扩展的设计分析。*
